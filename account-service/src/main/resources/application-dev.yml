spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/account_db
    username: your_name
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

#eureka:
#  client:
#    enabled: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#      instance:
#        prefer-ip-address: true
